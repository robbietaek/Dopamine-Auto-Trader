<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dopamine.trade.dao.OrderDao">

  <insert id="insertOrderInformation" parameterType="com.dopamine.api_call.model.response.order.order.Order">
    INSERT INTO
      tb_coin_order_history
    (
      uuid
      , order_side
      , order_type
      , market
      , order_time
    )
    VALUES
    (
      #{order.uuid}
      , #{order.side}
      , #{order.ordType}
      , #{order.market}
      , NOW()
    )
  </insert>

  <select id="selectOrderHistory" resultType="com.dopamine.trade.model.OrderHistory">
    SELECT
      uuid
      , order_side
      , order_type
      , market
      , order_time
      , volume
      , executed_volume
      , remaining_volume
    FROM
      tb_coin_order_history
  </select>

</mapper>